# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

alias sudo='sudo '
alias l='ls'
alias c='clear'
alias x='exit'
alias q='exit'

export GTK_IM_MODULE=xim
export XMODIFIERS="@im=fcitx"
export QT_IM_MODULE=xim

# User specific aULE=liases and functions
